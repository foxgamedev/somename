# Не чіпати вміст файлів (жодної конвертації переносів рядків).
# Критично для роздачі через лаунчер: байти в git мають збігатися з локальними,
# інакше sha256 у маніфесті не зійдеться з тим, що віддає GitHub.
* -text
